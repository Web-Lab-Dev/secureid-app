/**
 * Centralisation des icônes Lucide React
 * Permet d'importer les icônes depuis un seul endroit
 * et facilite les changements futurs
 */

export {
  // Icônes d'authentification
  Eye as ShowIcon,
  EyeOff as HideIcon,
  Phone as PhoneIcon,
  Lock as LockIcon,
  User as UserIcon,

  // Icônes de sécurité
  Shield as ShieldIcon,

  // Icônes d'action
  UserPlus as UserPlusIcon,
  Users as UsersIcon,
  LogOut as LogOutIcon,

  // Icônes d'état
  Loader2 as LoaderIcon,
  AlertCircle as AlertIcon,
  CheckCircle as CheckIcon,
  XCircle as ErrorIcon,

  // Icônes de navigation
  ChevronLeft as BackIcon,
  ChevronRight as NextIcon,
  X as CloseIcon,

  // Type pour toutes les icônes
  type LucideIcon
} from 'lucide-react';
