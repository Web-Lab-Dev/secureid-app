import { ScanPageClient } from './page-client';

/**
 * PAGE DE SCAN DE QR CODE
 *
 * Permet au parent de scanner un nouveau bracelet
 * pour protéger un autre enfant
 */

export default function ScanPage() {
  return <ScanPageClient />;
}
